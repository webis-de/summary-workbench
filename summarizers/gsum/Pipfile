[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
torch = "==1.5.0"
numpy = "<1.24"
requests = "*"
tqdm = "*"
regex = "*"

[dev-packages]

[requires]
python_version = "3.8"  # 3.9 or higher is not supported because of torch 1.5 dependency
