kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: summarizer-mongodb-data
  labels:
    app: summarizer
    tier: mongodb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-rbd
