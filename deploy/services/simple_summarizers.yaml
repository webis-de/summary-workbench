apiVersion: apps/v1
kind: Deployment
metadata:
  name: summarizer-simple-summarizers
  labels:
    app: summarizer
    tier: simple-summarizers
spec:
  selector:
    matchLabels:
      app: summarizer
      tier: simple-summarizers
  template:
    metadata:
      labels:
        app: summarizer
        tier: simple-summarizers
    spec:
      containers:
        - name: simple-summarizers
          image: docker.io/ds40bamo/simple_summarizers:1.0
          imagePullPolicy: Always
          command: ["bash", "-c", "gunicorn wsgi:app"]
          env:
            - name: GUNICORN-CMD-ARGS
              value: "-cgunicorn.conf.py"
          ports:
            - name: s-summ-port
              containerPort: 5000
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /health
              port: s-summ-port
            periodSeconds: 60
            timeoutSeconds: 60
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: summarizer-simple-summarizers
spec:
  selector:
    app: summarizer
    tier: simple-summarizers
  ports:
    - port: 5000
      targetPort: s-summ-port
